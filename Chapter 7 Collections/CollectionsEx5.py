#How do the set types differ from the sequence type (strings, tuples, lists)?

#Set types are unordered, so they don't support indexing, but sequences types are.
#Sets do not allow duplicate values

myset = {'a', 'b', 'c'}
print(myset)
print(type(sorted(myset)))